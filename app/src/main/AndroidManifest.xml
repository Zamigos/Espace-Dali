<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zamigos.espacedali">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CommentActivity"
            android:label="@string/activity_title_comment"
            android:parentActivityName=".DetailActivity" />
        <activity
            android:name=".ThemeActivity"
            android:label="@string/activity_title_theme"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".OeuvreActivity"
            android:label="@string/activity_title_oeuvre"
            android:parentActivityName=".ThemeActivity" />
        <activity
            android:name=".DetailActivity"
            android:label="@string/activity_title_detail"
            android:parentActivityName=".OeuvreActivity" />
        <activity android:name=".FavoriteActivity"></activity>
    </application>

</manifest>
